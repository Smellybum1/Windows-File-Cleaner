# Thread Handoff

Last updated: 2026-06-02

Use this when starting a fresh Codex thread for this repository.

## Current state

- Repo: `D:\Codex\Windows File Cleaner`
- GitHub: `Smellybum1/Windows-File-Cleaner`
- Branch: `main`
- Latest completed packet: Shortcut Installer Deferral Decision
- Current app stack: C# / WPF / .NET 8
- Daily accepted package path: use `.\tools\Start-AcceptedLocalRelease.cmd -PrintOnly` or `.\tools\Start-AcceptedLocalRelease.cmd -Fixture -PrintOnly` to print accepted package launch commands.
- The accepted launcher output repeats that debug-build desktop shortcuts are development-only references, not the accepted daily path.
- ADR 0020 keeps accepted package launch commands as the v1 daily path and defers installed shortcut or installer automation to a later explicit user-approved packaging packet.
- Debug desktop shortcut target, if present: `D:\Codex\Windows File Cleaner\src\WindowsFileCleaner.App\bin\Debug\net8.0-windows\WindowsFileCleaner.App.exe`

## Product status

Windows File Cleaner is a local Windows-only desktop app for reviewing storage under `C:\Users\moxhe`.

The current MVP has a read-only Storage Scan that can inspect large real-profile scans with filters, compact header scan totals for total size / folders / files / access issues, compact header Review Shortlist totals for shortlisted size / folder rows / file rows, a horizontal WPF tabbed workbench for Safety Summary, Review, Quarantine, and Main Grid pages with Safety Summary shortcut and current-session grid-switch actions auto-focusing Main Grid after they change visible rows, Main Grid Active Review Lens Summary above Storage Scan rows mirroring active filters/search/focus and hiding for current-session quarantined rows, a compact wrapping scan-header status strip for Cleanup Scope Safety Note / scan-gate summary / scan-gate detail, dedicated Safety Summary and Quarantine pages with panel-name header summaries plus mirrored header tooltip/help text, visible hoverable header `?` help cues, state-naming header tooltip/help text, and Safety Summary/Quarantine Shortlist header state styling, semantically styled inline Quarantine Preview readiness/status in the Quarantine tab with state-naming mirrored tooltip/help text and a visible hoverable `?` help cue, compact Quarantine Readiness Summary text with mirrored `Summary state:` tooltip/help text, a `Remove overlapping parents` action that fixes redundant parent/child preview blockers by removing broader parent rows from Review Shortlist and requiring a fresh preview, Quarantine Execution Gate tooltip/help text with a visible hoverable `?` help cue, exact-confirmation field tooltip/help text exposed through shortlist confirmation and selected restore confirmation visible hoverable `?` help cues, Quarantine Manifest Discovery, Restore Manifest selection, all-manifest readiness tooltip/help text exposed through visible hoverable `?` help cues, a compact Restore Manifest review summary with `Summary state:` tooltip/help text, Selected Restore Execution Gate tooltip/help text with a visible hoverable `?` help cue, semantically styled Review Grid Mode Status above the main grid with state-naming mirrored tooltip/help text and a visible hoverable `?` help cue, Cleanup Scope Safety Note, Quarantine Root Safety Note, and Non-D Quarantine Root Acknowledgement help text plus visible hoverable `?` help cues, WPF smoke coverage for the nineteen hoverable `?` help-cue affordance settings, current-session quarantined-items grid switching with dynamic `Current quarantined (N)` / `Back to scan rows` help text, ADR 0016 boundary keeping older/discovered Restore Manifests in manifest discovery/readiness panes instead of the `Current quarantined` main-grid switch, ADR 0017 Real-Profile Quarantine Readiness Contract, shortlist-level fixture Quarantine wording, preserves current-fixture undo after a post-execution rescan, manual fixture Show children/Clipboard crash fix, scan cancel help text, scan-gate summary help text plus a visible hoverable `?` help cue, real-profile acknowledgement help text plus a visible hoverable `?` help cue, Review Mix, Matched Review Mix, and Review Shortlist Safety Mix hoverable `?` help cues with mirrored tooltip/help text, Safety Summary shortcut help text, review-lens filter help text, manifest discovery/selection help text, debounced search and search-input automation help text, scan-gate automation help text, Cleanup Scope input/browse automation help text, Quarantine Root input/browse automation help text, selected-row action automation help text, visible-row shortlist automation help text, execution/readiness automation help text, review report/preview automation help text, review toolbar automation help text, review navigation/export tooltip clarity, visible scope-specific Cleanup Scope Scan Gate status/tooltip wording, Cleanup Scope and Quarantine Root browse tooltip clarity, selected-row action tooltip clarity, Review Shortlist Safety Mix mirrored tooltip/help text, visible-row Review Shortlist bulk labels/tooltips, review toolbar report/preview tooltip clarity, selected-folder child/descendant focus, selected-folder summaries, hotspot trail, file preview, CSV export, Review Shortlist, fixture launcher checklist output aligned with hoverable `?` help-cue wording, checklist-only mode now covered by MVP preflight, and execution-policy-friendly `.cmd` wrappers for human-facing PowerShell tools aligned with recent review polish, Quarantine Preview and Quarantine Execution Gate scope-status/approval-boundary wording without technical implementation-flag wording, execution-control tooltip clarity, readiness scope tooltip clarity, Restore Manifest Draft, Quarantine Confirmation Draft, confirmation label wording polish, Quarantine Action Draft, manifest discovery with all-manifest restore wording, Restore Manifest wording polish, selected manifest readiness label polish, all-manifest readiness label polish, selected manifest review with readiness-evidence wording, selected restore gate scope-status/approval-boundary wording without technical implementation-flag wording, selected-restore scope-status checklist coverage, all-manifest readiness preview with all-manifest restore wording, and all-manifest restore boundary checklist coverage.

Fixture cleanup execution exists for synthetic Cleanup Scopes. Exact real-profile selected restore exists for one selected `C:\Users\moxhe` Restore Manifest after selected readiness, exact `RESTORE`, and immediate revalidation. First-phase exact real-profile Quarantine execution now exists for `C:\Users\moxhe` only after ADR 0017/0018 readiness, exact `QUARANTINE`, Real-Profile Quarantine Approval Evidence, selected real-profile restore trust, and immediate pre-execution revalidation pass. Codex and automated tests do not click this real-profile movement path.

Portable v1 packaging exists through `tools\Publish-LocalRelease.cmd`. It runs MVP preflight by default, publishes a self-contained `Release` / `win-x64` WPF app under ignored `.local\releases\windows-file-cleaner-vYYYYMMDD-HHMMSS\app`, writes local release metadata with executable SHA-256, creates a zip plus sibling `.zip.sha256` sidecar, prints exact launch commands, and writes release-local `README-FIRST.txt`, `Launch-WindowsFileCleaner.cmd`, and `Launch-WindowsFileCleaner-Fixture.cmd` artifacts. `README-FIRST.txt` travels with the folder and zip as package-local start-here guidance for launch choices and the reversible-only v1 boundary. `tools\Test-LocalRelease.cmd` verifies the latest or explicit local package folder, executable, metadata, executable checksum, README, launch scripts, zip, zip checksum sidecar, safety-boundary lines, and commit evidence without launching WPF or scanning anything. `tools\Start-LocalRelease.cmd` verifies by default and can print, checklist-review, write ignored acceptance notes, or start the latest package from the repo root; its output now also prints the package-local README and matching release-local launch script path. `-ChecklistOnly` prints package-level normal/fixture acceptance steps and exact launch commands without launching WPF, clicking `Scan`, moving, restoring, deleting, approving cleanup, or creating cleanup history. `-ChecklistOnly -WriteAcceptanceNotes` writes ignored notes under `.local\release-acceptance`; those notes pre-record verifier/current-commit evidence only when the launcher has proven it, stamp exact normal and fixture launch commands, and keep launch and fixture scan evidence manual. `tools\Summarize-LocalReleaseAcceptanceNotes.cmd` can summarize those notes and print the stamped launch commands without launching WPF or scanning. `tools\Record-LocalReleaseAcceptanceNotes.cmd -RecordManualAcceptance` can mark ignored notes complete after the human package acceptance pass; it writes ignored notes only and does not launch WPF, scan, move, restore, delete, approve cleanup, or create cleanup history. `tools\Start-AcceptedLocalRelease.cmd` requires completed ignored acceptance notes, selects the accepted release folder, and delegates to `Start-LocalRelease.cmd` so daily accepted-package use is not blocked by later docs-only commits. Its output now names the accepted release launch command as the daily path instead of any debug-build desktop shortcut, while confirming that it does not create shortcuts or install anything. `tools\Invoke-DailyLocalReadiness.cmd` composes completed accepted-notes verification, optional Fixture Acceptance Notes summary/completion evidence, one accepted package verification pass, accepted normal/fixture print-only launch commands, and Restore Manifest Summary output into one read-only daily check. `-Fixture` only prefills the repo-local smoke fixture Cleanup Scope. Current accepted local package baseline is `.local\releases\windows-file-cleaner-v20260602-011556` at commit `bc9b869`, with completed ignored notes at `.local\release-acceptance\release-acceptance-20260602-011743.md`; `tools\Summarize-LocalReleaseAcceptanceNotes.cmd -RequireComplete` reports verifier/current-commit/normal-launch/fixture-launch evidence recorded, overall result `Pass`, and `6 pass, 0 issue, 0 not checked, 0 not recorded`. It is not an installer and does not add installed shortcuts, permanent deletion, persisted cleanup history, broad/all-manifest restore, custom real-profile Quarantine, or non-exact real-profile movement. Any local desktop shortcut that still points at the debug build is development-only context, not accepted package evidence or the recommended daily launch path. ADR 0020 keeps accepted package launch commands as the v1 daily path and requires a later explicit user-approved packaging packet before creating installed shortcuts or installer behavior.

`tools\Invoke-RealProfileQuarantineReadiness.cmd` runs full MVP preflight by default, then daily local readiness plus focused Restore Manifest recovery-review and undo-work summaries before any future tiny exact real-profile Quarantine batch review. Restore Manifest display focus defaults to exact `C:\Users\moxhe`; use `-RequireNextBatchEvidence` for the recommended next-batch preset that includes Fixture Acceptance Notes status, requires exact-profile displayed Restore Manifest evidence, and requires zero displayed undo-work manifests. Use `-AllCleanupScopes` only when fixture manifests should also be displayed outside that preset. Displayed strictness flags such as `-RequireAnyDisplayedRestoreManifest -RequireNoDisplayedUndoWork` can still enforce exact-profile displayed evidence separately from full-root fixture history, and `-IncludeFixtureAcceptanceNotes` / `-RequireFixtureAcceptanceComplete` can forward optional Fixture Acceptance Notes evidence through daily readiness. `tools\Show-RealProfileNextBatchChecklist.cmd` prints the post-preset manual WPF checklist without launching WPF and now reminds the reviewer to confirm Fixture Acceptance Notes status from the preset, use the printed recorder command only after an actual all-pass visible fixture review, or fill notes manually for issues/not-checked items. `tools\Invoke-RealProfileNextBatchReview.cmd` runs the exact-profile preset and then prints that checklist in one terminal-only flow; `-SkipMvpPreflight` is for smoke checks only, not fresh movement evidence. These commands are terminal-only guidance/evidence and do not launch WPF, scan the real profile, move, restore, delete, approve cleanup, write Restore Manifests, or create cleanup history.

Restore Manifest terminal summary exists through `tools\Summarize-RestoreManifests.cmd`. It reads action-scoped `actions\*\restore-manifest.json` under the selected Quarantine Root, prints manifest counts, entry status counts, size, cleanup scopes, undo-work and recovery-review flags, displayed strictness counts when display filters or displayed strict flags are active, optional entry-level paths, and the selected-only/no-all-manifest restore boundary. `-CleanupScope` focuses displayed manifests by exact Cleanup Scope, `-RecoveryReviewOnly` focuses displayed manifests that need recovery review, `-UndoWorkOnly` focuses displayed manifests with moved entries still needing selected restore/undo review, `-RequireNoRecoveryReview` / `-RequireNoUndoWork` fail read-only terminal evidence checks against full selected-root state, and `-RequireAnyDisplayed` / `-RequireNoDisplayedRecoveryReview` / `-RequireNoDisplayedUndoWork` fail against only the displayed manifest set. It is read-only and does not launch WPF, scan, move, restore, delete, write manifests, approve cleanup, or create cleanup history.

Latest daily evidence refresh on current `main` at `5b2a982` passed `tools\Invoke-DailyLocalReadiness.cmd -IncludeFixtureAcceptanceNotes -CleanupScope "C:\Users\moxhe" -RequireAnyDisplayedRestoreManifest -RequireNoDisplayedUndoWork`. Accepted package notes are complete, optional Fixture Acceptance Notes status still shows the latest clean ignored fixture notes are formally unfilled, accepted package verification still passes with the expected accepted-package/current-HEAD warning, accepted normal and fixture launch commands print without launching WPF, the accepted launcher output repeats the accepted-release daily-path/debug-shortcut boundary, exact-profile Restore Manifest display shows 4 of 10 manifests, displayed exact-profile undo work is 0, and displayed exact-profile recovery review is 2. This was terminal evidence only: no WPF launch, scan, movement, restore, deletion, approval, manifest write, or cleanup history.

Latest fixture-notes status: `tools\Invoke-DailyLocalReadiness.cmd -IncludeFixtureAcceptanceNotes -CleanupScope "C:\Users\moxhe" -RequireAnyDisplayedRestoreManifest -RequireNoDisplayedUndoWork` passed and printed optional Fixture Acceptance Notes status without launching WPF, scanning, moving, restoring, deleting, approval, or cleanup history. Strict `tools\Invoke-DailyLocalReadiness.cmd -RequireFixtureAcceptanceComplete` failed as expected because the latest ignored fixture notes remain formally unfilled: preflight/worktree evidence not recorded, overall result not recorded, and 10 checklist items not recorded. `tools\Summarize-FixtureAcceptanceNotes.cmd` now prints the exact `Record-FixtureAcceptanceNotes.cmd -Path ... -RecordManualAcceptance` follow-up when notes are incomplete, but that command remains for after an actual all-pass visible fixture review only.

Latest full real-profile next-batch evidence refresh: `tools\Invoke-RealProfileQuarantineReadiness.cmd -RequireNextBatchEvidence` passed again on current `main` at `d257090` after checklist Fixture Acceptance Notes guidance. Full MVP preflight restored, built, ran core tests, ran WPF app tests, ran fixture `-WhatIf`, printed checklist-only fixture guidance, and ran whitespace diff checking. Daily readiness then verified completed accepted package notes, printed optional fixture notes status, ran one accepted package verifier pass with the expected accepted-package/current-HEAD warning, printed accepted normal/fixture launch commands in print-only mode, and showed exact-profile Restore Manifest display with 4 of 10 manifests, displayed undo work 0, and displayed recovery review 2. Focused recovery-review evidence still shows the two older failed exact-profile NVIDIA `DXCache` attempts, and focused undo-work evidence shows zero exact-profile matches. This was terminal evidence only: no WPF launch, real-profile scan, movement, restore, deletion, approval, manifest writes, or cleanup history.

Latest next-batch checklist wrapper-hint evidence: `tools\Show-RealProfileNextBatchChecklist.cmd` passed and now prints `tools\Invoke-RealProfileNextBatchReview.cmd` as the recommended combined terminal review before the separate evidence preset. `tools\Invoke-RealProfileNextBatchReview.cmd -SkipMvpPreflight` also passed as a smoke check after the hint change, with skipped-preflight warning and no awkward command failure. These were terminal evidence/guidance only: no WPF launch, real-profile scan, movement, restore, deletion, approval, manifest write, or cleanup history.

Latest next-batch checklist fixture-notes guidance: `tools\Show-RealProfileNextBatchChecklist.cmd` now prints a `Before launch` reminder to confirm Fixture Acceptance Notes status from the evidence preset, use the printed recorder command only after an actual all-pass visible fixture review, and fill notes manually for issues/not-checked items. The standalone checklist and skipped-preflight wrapper smoke passed without WPF launch, scan, movement, restore, deletion, approval, manifest write, or cleanup history.

Latest full next-batch evidence after checklist guidance: `tools\Invoke-RealProfileQuarantineReadiness.cmd -RequireNextBatchEvidence` passed on `d257090` with full MVP preflight, accepted package evidence, optional Fixture Acceptance Notes status, exact-profile Restore Manifest display, recovery-review focus, and undo-work focus. The accepted package/current-HEAD warning remains expected because the accepted package is `.local\releases\windows-file-cleaner-v20260602-011556` at `bc9b869`; current `HEAD` is docs/tooling-only beyond that package baseline.

Latest exact-profile recovery-review entry evidence: `tools\Summarize-RestoreManifests.cmd -CleanupScope "C:\Users\moxhe" -RecoveryReviewOnly -ShowEntries` passed and showed the two displayed recovery-review manifests are failed-only NVIDIA `DXCache` directory attempts with moved count 0, undo work no, and recovery review yes. `quarantine-action-draft-20260601110130-9010b9d9` failed because a descendant `.nvph` file was in use by another process; `quarantine-action-draft-20260601103558-b3e00fc2` failed because source and destination roots differed before the guarded cross-volume fallback existed. Exact-profile undo-work focus still shows zero matches, and `-RequireAnyDisplayed -RequireNoDisplayedUndoWork` passed with 4 exact-profile displayed manifests, displayed undo work 0, and displayed recovery review 2. This was read-only terminal evidence only: no WPF launch, real-profile scan, movement, restore, deletion, approval, manifest writes, or cleanup history.

Latest current-head daily readiness after recovery evidence: `tools\Invoke-DailyLocalReadiness.cmd -IncludeFixtureAcceptanceNotes -CleanupScope "C:\Users\moxhe" -RequireAnyDisplayedRestoreManifest -RequireNoDisplayedUndoWork` passed on `5b2a982`. It verified completed accepted package notes, printed optional Fixture Acceptance Notes status, ran one accepted package verifier pass with the expected accepted-package/current-HEAD warning (`bc9b869` accepted package versus `5b2a982` current `HEAD`), printed accepted normal/fixture launch commands in print-only mode, and showed exact-profile Restore Manifest display `(4 of 10)`, displayed undo work 0, and displayed recovery review 2. This was read-only terminal evidence only: no WPF launch, scan, movement, restore, deletion, approval, manifest writes, or cleanup history.

Latest shortcut/installer decision: ADR 0020 accepted the deployment boundary that accepted package launch commands remain the v1 daily path. Installed shortcut or installer automation stays unavailable unless a later explicit user-approved packaging packet defines target selection, stale-target handling, created artifacts, removal behavior, and read-only verification. This packet did not create shortcuts, install anything, publish a package, launch WPF, scan, move, restore, delete, approve cleanup, write Restore Manifests, or create cleanup history.

The latest WPF UI packet added Main Grid Active Review Lens Summary above Storage Scan rows, mirroring the existing Filter Summary for default scan, Safety Summary shortcut, no-category shortcut, and stacked shortcut/search states, while hiding for current-session quarantined rows. User visual review on 2026-06-01 approved the current wide header layout with Review Shortlist totals before scan totals. The latest fixture-checklist wording packet clarified that the current-session review step's hoverable `?` cue and `Status state:` wording belong to Review Grid Mode Status, while Main Grid Active Review Lens Summary appears for Storage Scan rows and hides for current-session quarantined rows. The live-product readiness roadmap now separates visible fixture acceptance, fresh real-profile read-only retest, selected real-profile restore, first real-profile Quarantine execution, recovery confidence, packaging, and later deletion/history decisions without enabling any movement, and its manual fixture acceptance row names the embedded-command notes workflow plus current full-preflight-after-checklist-only-next-step state. The fixture launcher can now write ignored `.local` fixture acceptance notes from the same checklist item source with `-WriteAcceptanceNotes`; notes are grouped by fixture review area and now include repository path, Git branch/commit, worktree status at notes creation, .NET SDK, WPF app project/target framework/WPF flag, preflight command, visible fixture command, preflight/worktree checkboxes, local-not-cleanup-history wording, and embedded exact recorder/summary/completion-check commands. The latest full `.cmd` MVP preflight ran after the Checklist-Only Visible Fixture Next Step packet at `71cf15a` and passed restore, build, core tests, WPF app tests, fixture `-WhatIf`, sectioned fixture checklist-only output with the exact visible fixture next-step block, and whitespace diff without scanning or modifying real user files; its success output points to the notes-enabled `.\tools\Start-MvpFixtureReview.cmd -SkipPreflight -WriteAcceptanceNotes` next step and reminds the user to use the printed summary commands after filling notes. A fresh checklist-only notes preview wrote `.local\fixture-review-acceptance\fixture-acceptance-20260601-131233.md` from commit `dd86566`, with `.NET SDK: 8.0.421`, the WPF project, `net8.0-windows`, `WPF enabled: true`, and `Worktree status at notes creation: clean`, without launching WPF or touching real-profile files. The read-only summary helper can summarize latest or explicit ignored fixture acceptance notes so the next visible pass can quickly surface worktree status at notes creation, acceptance-evidence checkbox states, overall result, checklist totals, and issue/not-checked/not-recorded items with compact notes or prompt previews before progress docs are updated. Its `-RequireComplete` mode exits non-zero until the local notes have recorded preflight/worktree evidence, an overall result, and no not-checked or not-recorded checklist items; incomplete summaries now print the exact recorder command while preserving the all-pass-human-review-only boundary. `tools\Record-FixtureAcceptanceNotes.cmd -RecordManualAcceptance` can mark ignored all-pass fixture notes complete after a human visible pass; it updates ignored `.local` notes only and does not launch WPF, create fixtures, scan, move, restore, delete, approve cleanup, or create cleanup history. The fixture launcher also prints the exact `-Path` recorder, summary, and `-RequireComplete` commands for the notes file it writes.

Checklist-only launcher output also repeats the exact notes-enabled visible fixture command and the no-preflight/no-fixture/no-WPF/no-scan/no-movement boundary before exiting.

User-reported fixture acceptance now exists: on 2026-06-01 the user ran the visible fixture review flow and reported that it looks good. Fresh clean-worktree notes evidence also exists at `.local\fixture-review-acceptance\fixture-acceptance-20260601-132126.md`, generated by the visible launcher from commit `433064e` with `Worktree status at notes creation: clean`. The read-only summary helper printed the clean stamp and shows the checklist is still unfilled, so this is user-reported visual acceptance rather than formal completed checklist evidence until the user fills notes manually or intentionally runs the printed `Record-FixtureAcceptanceNotes.cmd -RecordManualAcceptance` command for an all-pass fixture review.

User-reported fresh real-profile read-only retest now exists: on 2026-06-01 the user completed the requested full preflight plus WPF run against `C:\Users\moxhe` and reported that everything worked well. The requested pass covered the real-profile scan gate, real-profile acknowledgement, read-only scan completion, search responsiveness, tab/header usability, Review Shortlist context, and preview-only Quarantine boundary. After that, the user explicitly approved the ADR 0019 selected real-profile restore implementation path. The user then completed the sacrificial selected real-profile restore trust test and reported steps 1-9 all succeeded.

The manual selected-restore trust helper exists: `.\tools\New-RealProfileSelectedRestoreTrustManifest.cmd` creates one sacrificial exact real-profile Restore Manifest under `D:\WindowsFileCleanerQuarantine` by default and targets `C:\Users\moxhe\WindowsFileCleanerRestoreTrustTest\restore-target.txt`. It refuses to run if the restore target already exists and does not quarantine existing personal files. Use it only after full preflight and explicit user intent to click the restore test in WPF.

First real-profile Quarantine implementation, first tiny-batch live trust evidence, and selected restore recovery proof now exist: core tests and WPF app tests pass; synthetic exact real-profile missing-source execution attempts rerun immediate pre-execution revalidation and report no movement; custom and real-profile-child scopes remain preview-only. The user attempted a first exact real-profile folder Quarantine on 2026-06-01 and reported the app failed safely with `moved 0, failed 1` because `Directory.Move` cannot move directories from `C:` to the preferred `D:` Quarantine Root. The next packet added a guarded cross-volume directory copy-then-delete fallback under `QuarantineExecutor`. The user retried and reported another safe `moved 0, failed 1` result because a descendant NVIDIA `.nvph` cache file was in use by another process. The next packet added source/file-in-use access checks to Pre-Execution Revalidation and expanded the WPF Quarantine Execution Gate details area again. The user then approved a different tiny exact real-profile batch and reported success: one `C:\Users\moxhe\AppData\Local\pip\cache\http\b\c` row moved to `D:\WindowsFileCleanerQuarantine\actions\quarantine-action-draft-20260601112432-ab98a4a0\items\AppData\Local\pip\cache\http\b\c`, Restore Manifest `restore-manifest-20260601112432-a715565e` completed, `moved 1, failed 0`, zero blockers, and no recovery review needed. The user tried selected restore for that manifest and reported `Restored 0 | Failed 1 | Recovery review: yes` because directory restore from `D:` back to `C:` hit the same cross-volume limitation. The next packet routed directory restore through the guarded copy-then-delete fallback, treated still-present `RestoreFailed` selected entries as narrowly retryable when the original path is clear, and added highlighted key-status strips above the dense Quarantine and Selected Restore gate text; follow-up selected-restore highlight polish explicitly surfaced `Can execute: yes`, exact `RESTORE` matched, and real-profile `Can proceed: yes`. The user then retried selected restore for that first-live manifest and reported success from the highlighted strip: `selected restore succeeded. Restored 1, failed 0`. The latest UI packet moved `Discover manifests`, selected readiness, selected restore gate/result, and all-manifest readiness out of Main Grid and into a dedicated `Restore Manifest Review` panel inside the Quarantine tab. The user then completed the requested follow-up and confirmed rediscovery showed the manifest restored/already restored, rescan completed normally, and the restored `pip\cache\http\b\c` path appeared again.

## Safety boundary

- Do not run real-profile file movement without explicit user approval for the specific action. Implemented real-profile movement paths are exact first-phase Quarantine for `C:\Users\moxhe` and ADR 0019 selected restore for one exact `C:\Users\moxhe` Restore Manifest.
- Do not enable real-profile file movement by flipping a broad implementation flag; ADR 0017 and ADR 0018 require exact scope, readiness, approval evidence, and immediate revalidation.
- Do not treat `QuarantineExecutionReadiness` as sufficient execution approval by itself; WPF exact real-profile execution also requires exact `QUARANTINE`, Real-Profile Quarantine Approval Evidence, and immediate revalidation.
- Do not treat `RealProfileQuarantineApprovalEvidence` or `Invoke-RealProfileQuarantineReadiness.cmd` output as cleanup approval or a persisted approval record; each is only one evidence input before the exact real-profile execution guard.
- Do not wire additional movement paths without updating the source guard, tests, and safety docs.
- Do not treat `QuarantineRootExecutionSafety` as execution approval; it is one input to readiness and does not move files or write manifests.
- Do not treat `PreExecutionRevalidation` as execution approval; it is one input to readiness and does not move files or write manifests.
- Treat in-use source file or folder-descendant blockers as real readiness blockers; do not force-close processes or bypass them from Codex.
- Do not treat `RealProfileRestoreReadiness` as restore or quarantine approval; it is one input to readiness and does not move files or write manifests.
- Do not treat `SelectedRestorePreExecutionRevalidation` as restore approval; it is a non-moving rediscovery/revalidation model and must run again immediately before selected real-profile restore movement.
- Do not treat the Restore Manifest review summary as restore approval; it is a compact read-only orientation line for discovery/readiness/gate state and does not replace the detailed panes.
- Do not let read-only readiness/revalidation builders call `QuarantineExecutor.Execute`, `UndoQuarantineExecutor.Undo`, or `RestoreManifestFileStore.Write`; the core source guard now checks this boundary.
- Do not add WPF movement executor calls outside the known gated execution bridge methods unless a later explicit user-approved real-profile execution packet updates the source guard, tests, and ADR-backed safety contract.
- Keep cross-volume folder movement inside `QuarantineDirectoryMove` and `QuarantineExecutor`; do not copy/delete folders from WPF, readiness builders, or docs/testing helpers.
- Do not expand ADR 0019 selected restore into all-manifest restore. It restores one selected exact real-profile Restore Manifest only after selected readiness, exact `RESTORE`, and immediate revalidation.
- Do not implement permanent deletion as the next step.
- Keep Storage Scan read-only.
- Keep Review Shortlist as review context, not cleanup approval.
- Keep Quarantine Preview as a dry run until explicit execution gates are designed and tested.
- Treat browser profiles, credentials, game saves, cloud sync data, source code, active app settings, Codex/tooling caches, and broad `AppData` parents conservatively.

## Verified recently

- User confirmed real-profile scan works.
- User confirmed the real-profile scan gate checkbox enabled `Scan` after acknowledgement.
- User reported Search typing was sluggish on large real-profile results.
- `ce7c1db` added debounced user-typed Storage Review Search.
- User retested and confirmed Search typing feels much better.
- User confirmed the status-bar pending-search message is enough; do not add another indicator unless future testing contradicts this.
- User confirmed the tabbed/header UI now looks much better, including the compact header Review Shortlist totals positioned before the scan totals at the normal wide fixture window size.
- User ran the visible fixture review flow on 2026-06-01 and reported that it looks good; the latest ignored acceptance notes are stamped clean from `433064e` but remain unfilled.
- User completed the fresh real-profile read-only retest on 2026-06-01 and reported everything worked well, including the requested preflight/app launch/scan/search/UI/preview-only checks.
- Real-Profile Selected Restore Execution implemented ADR 0019 for one selected exact `C:\Users\moxhe` Restore Manifest; core tests and WPF app tests passed, and WPF tests do not click real-profile restore or write into the real profile.
- Real-Profile Selected Restore Trust Helper added `.\tools\New-RealProfileSelectedRestoreTrustManifest.cmd`; `-WhatIf` passed and a smoke run against ignored `.local\real-profile-selected-restore-trust-helper-smoke` generated ISO timestamp JSON without writing into the real profile.
- User completed the selected real-profile restore trust test on 2026-06-01 and reported steps 1-9 all succeeded.
- User completed the first exact real-profile Quarantine live trust batch on 2026-06-01 and reported one row moved, one Restore Manifest completed, `moved 1, failed 0`, and zero readiness blockers.
- User attempted selected restore for that first-live manifest and reported a safe cross-volume directory restore failure; after the fixed build and highlight polish, the user retried and reported selected restore success with `Restored 1, failed 0`, then confirmed rediscovery showed restored/already restored, rescan completed normally, and the restored `pip\cache\http\b\c` path appeared again.
- User reported `Discover manifests` was hard to find because it was in Main Grid; the latest packet moves Restore Manifest Review into the Quarantine tab and WPF smoke tests prove it is not in Main Grid.
- User completed the portable v1 verification pass from clean `main`: publisher succeeded, packaged app opened from the printed fixture launch command, and fixture Scan completed normally.
- Current accepted local package baseline is `.local\releases\windows-file-cleaner-v20260602-011556` at commit `bc9b869`, with completed ignored acceptance notes `.local\release-acceptance\release-acceptance-20260602-011743.md`; `tools\Summarize-LocalReleaseAcceptanceNotes.cmd -RequireComplete` reports verifier/current-commit/normal-launch/fixture-launch evidence recorded, overall result `Pass`, and `6 pass, 0 issue, 0 not checked, 0 not recorded`.
- Accepted Package Daily Use Guide added a top-level README section for `Start-AcceptedLocalRelease.cmd -PrintOnly`, fixture print-only launch, accepted-notes completion check, read-only Restore Manifest summary, and the stop boundary before real-profile movement.
- Daily Local Readiness Check added `tools\Invoke-DailyLocalReadiness.cmd`, which verifies accepted package notes, prints accepted normal/fixture launch commands, and prints Restore Manifest Summary output without creating shortcuts, installing anything, launching WPF, scanning, moving, restoring, deleting, approving cleanup, or creating cleanup history.
- Daily Readiness Single Package Verification made `tools\Invoke-DailyLocalReadiness.cmd` run the accepted package verifier once, then skip duplicate package verification only for the fixture print-only command in the same already-verified daily run.
- Daily Readiness Fixture Acceptance Status added `-IncludeFixtureAcceptanceNotes`, `-FixtureAcceptanceNotesPath`, and `-RequireFixtureAcceptanceComplete` to daily readiness, and forwarded those flags through real-profile readiness. The default daily command remains unchanged.
- Full Real-Profile Readiness After Fixture Status reran the full evidence path on `ceed1fc` with optional fixture notes status and exact-profile displayed strictness; preflight, accepted package evidence, exact-profile Restore Manifest display, recovery-review focus, and undo-work focus all passed without WPF launch, real-profile scan, movement, restore, deletion, approval, or cleanup history.
- Real-Profile Next-Batch Evidence Preset added `tools\Invoke-RealProfileQuarantineReadiness.cmd -RequireNextBatchEvidence` as the exact-profile-only preset before another tiny exact batch review; it includes Fixture Acceptance Notes status, requires exact-profile displayed Restore Manifest evidence, requires zero displayed undo-work manifests, rejects all-scope or fixture-scope use, and passed with full MVP preflight without WPF launch, real-profile scan, movement, restore, deletion, approval, or cleanup history.
- Real-Profile Next-Batch WPF Checklist added `tools\Show-RealProfileNextBatchChecklist.cmd`, which prints exact-scope, tiny-batch, hard-blocker, Quarantine tab evidence, human-only click, and rediscover/rescan guidance without launching WPF, scanning, movement, restore, deletion, approval, or cleanup history.
- Next-Batch Checklist Fixture Notes Guidance added a `Before launch` reminder to `tools\Show-RealProfileNextBatchChecklist.cmd` so fixture notes status and incomplete-notes handling are visible before any future WPF batch review, without launching WPF, scanning, movement, restore, deletion, approval, manifest writes, or cleanup history.
- Full Next-Batch Evidence After Checklist Guidance reran `tools\Invoke-RealProfileQuarantineReadiness.cmd -RequireNextBatchEvidence` on `d257090`; full MVP preflight and terminal evidence passed without WPF launch, real-profile scan, movement, restore, deletion, approval, manifest writes, or cleanup history.
- Real-Profile Next-Batch Review Wrapper added `tools\Invoke-RealProfileNextBatchReview.cmd`, which runs the exact-profile evidence preset and then prints the WPF checklist without launching WPF, scanning, movement, restore, deletion, manifest write, approval, or cleanup history.
- Real-Profile Quarantine Readiness Review added `tools\Invoke-RealProfileQuarantineReadiness.cmd`, which runs full MVP preflight by default, then daily local readiness plus focused Restore Manifest recovery-review and undo-work summaries before any future tiny exact real-profile batch review, without launching WPF, scanning the real profile, moving, restoring, deleting, approving cleanup, or creating cleanup history.
- Restore Manifest Cleanup Scope Filter added `-CleanupScope` to the read-only Restore Manifest summary helper and forwarded it through daily/real-profile readiness wrappers so exact `C:\Users\moxhe` manifest evidence can be displayed separately from fixture manifests without restore/history behavior.
- Real-Profile Readiness Default Scope Focus made `tools\Invoke-RealProfileQuarantineReadiness.cmd` default Restore Manifest display focus to exact `C:\Users\moxhe`; `-AllCleanupScopes` preserves the previous fixture-inclusive display.
- Restore Manifest Displayed Strictness added displayed-only strict flags to the read-only Restore Manifest summary helper and forwarded them through daily/real-profile readiness wrappers so exact-profile displayed evidence can be enforced separately from fixture history.
- Fixture Acceptance Notes Recorder added `tools\Record-FixtureAcceptanceNotes.cmd -RecordManualAcceptance` so all-pass ignored fixture notes can be marked complete after a human visible pass without hand-editing markdown, WPF launch, scan, movement, restore, deletion, approval, or cleanup history.
- Current-Head Daily Readiness Evidence Refresh reran current read-only accepted-package, optional fixture-notes status, and exact-profile Restore Manifest evidence on `main` at `0bc2e96`; exact-profile display exists, has zero displayed undo-work manifests, and still has two displayed recovery-review manifests from older failed `DXCache` attempts.
- Restore Manifest Undo Work Filter added `-UndoWorkOnly` and `-RequireNoUndoWork` to the read-only Restore Manifest summary helper so outstanding moved entries can be focused or gated without restore/history behavior.
- Restore Manifest Recovery Review Filter added `-RecoveryReviewOnly` and `-RequireNoRecoveryReview` to the read-only Restore Manifest summary helper so recovery-review debt can be focused or gated without restore/history behavior.
- Fixture Acceptance Clean Worktree Notes Preview generated ignored checklist-only notes at `.local\fixture-review-acceptance\fixture-acceptance-20260601-122940.md` from `f181627` with `Worktree status at notes creation: clean`; explicit summary passed and explicit `-RequireComplete` failed as expected for unfilled notes, without WPF launch, scan, movement, restore, deletion, or cleanup history.
- Full Local MVP Preflight After Clean Notes Preview passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, sectioned fixture checklist-only output, and whitespace diff all passed after `8529a91` without launching WPF, scanning real-profile files, moving, restoring, deleting, or creating cleanup history.
- Full Local MVP Preflight After Current Evidence Wording Alignment passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, sectioned fixture checklist-only output, and whitespace diff all passed after `466ad79` without launching WPF, scanning real-profile files, moving, restoring, deleting, or creating cleanup history.
- Checklist-Only Visible Fixture Next Step updated `.\tools\Start-MvpFixtureReview.cmd -ChecklistOnly` so it prints the exact post-preflight visible fixture command and the checklist-only no-preflight/no-fixture/no-WPF/no-scan/no-movement boundary; checklist-only, checklist-notes, launcher `-WhatIf`, and focused preflight `-SkipRestore` output passed without launching WPF, scanning real-profile files, moving, restoring, deleting, or creating cleanup history.
- Full Local MVP Preflight After Checklist-Only Next Step passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, sectioned fixture checklist-only output with the exact visible fixture next-step block, and whitespace diff all passed after `71cf15a` without launching WPF, scanning real-profile files, moving, restoring, deleting, or creating cleanup history.
- Fixture Acceptance Current Baseline Notes Preview generated ignored checklist-only notes at `.local\fixture-review-acceptance\fixture-acceptance-20260601-131233.md` from `dd86566` with `Worktree status at notes creation: clean`; explicit summary passed and explicit `-RequireComplete` failed as expected for unfilled notes, without preflight, WPF launch, scan, movement, restore, deletion, or cleanup history.
- GitHub Actions MVP Preflight passed for `ce7c1db`: `https://github.com/Smellybum1/Windows-File-Cleaner/actions/runs/26616156540`
- Review Shortlist Safety Mix was added after the fresh-thread handoff docs so shortlist composition is visible before Quarantine Preview. It is read-only and not cleanup approval.
- Scan Gate Discoverability Polish added a visible locked/ready scan-gate summary and Scan button tooltip without changing the acknowledgement requirement.
- Scan Gate Summary Help Cue added a visible hoverable `?` cue beside the locked/ready scan-gate summary, mirrors dynamic summary tooltip/help text, and expands WPF smoke affordance coverage to nine cues.
- Quarantine Root Safety Note Help Cue added a visible hoverable `?` cue beside the Quarantine Root Safety Note, mirrors dynamic note tooltip/help text, and expands WPF smoke affordance coverage to ten cues.
- Cleanup Scope Safety Note Help Cue added a visible hoverable `?` cue beside the Cleanup Scope Safety Note, mirrors dynamic note tooltip/help text, and expands WPF smoke affordance coverage to eleven cues.
- Full Local MVP Preflight After Cleanup Scope Cue passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed without scanning or modifying real user files. The first sandboxed attempt failed on user-level NuGet config access, then the same command passed with approved access.
- Preflight Fixture Checklist Step added `Start-MvpFixtureReview.ps1 -ChecklistOnly` to the full MVP preflight path, with `-SkipFixtureChecklist` for focused loops; `cmd.exe /c tools\Invoke-MvpPreflight.cmd -SkipRestore` passed and printed the checklist without creating fixture files, launching WPF, or scanning real user files.
- Full Local MVP Preflight After Checklist Step passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output, and whitespace diff check all passed without scanning or modifying real user files.
- CI Preflight CMD Wrapper Alignment updated GitHub Actions to run `tools\Invoke-MvpPreflight.cmd` under `cmd`, matching the preferred local wrapper path; `cmd.exe /c tools\Invoke-MvpPreflight.cmd -SkipRestore` passed locally without scanning or modifying real user files.
- Quarantine Execution Label Docs Alignment updated current-facing manual-review docs to use the visible `Quarantine included shortlist` label instead of the older generic execution wording; docs-only, no scan or cleanup behavior changed.
- Selected Restore Gate Technical Wording removed the technical `Execution implemented` field from the selected restore gate pane while keeping fixture-only selected restore and custom/real-profile blockers unchanged; `dotnet run --project tests\WindowsFileCleaner.App.Tests\WindowsFileCleaner.App.Tests.csproj` passed.
- Quarantine Gate Technical Wording removed the technical `Execution implemented` field from Quarantine Preview and Quarantine Execution Gate panes while keeping fixture-only Quarantine execution and custom/real-profile blockers unchanged; `dotnet run --project tests\WindowsFileCleaner.App.Tests\WindowsFileCleaner.App.Tests.csproj` passed.
- Full Local MVP Preflight After Quarantine Gate Wording passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output, and whitespace diff check all passed without scanning or modifying real user files.
- Fixture Checklist Quarantine Button Label updated `Start-MvpFixtureReview.cmd -ChecklistOnly` step 7 to say `click Quarantine included shortlist`, matching the visible WPF action label; checklist-only output passed without preflight, fixture creation, WPF launch, scan, or file modification.
- Full Local MVP Preflight After Checklist Label passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, checklist-only output with `click Quarantine included shortlist`, and whitespace diff check all passed without scanning or modifying real user files.
- Quarantine Preview Placeholder Label updated WPF startup/reset placeholder text to say `Preview shortlist quarantine` instead of the older generic preview label; WPF smoke coverage now checks startup and stale-reset placeholders.
- Full Local MVP Preflight After Placeholder Label passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output, and whitespace diff check all passed without scanning or modifying real user files.
- Quarantine Execution Gate Preview Button Label translates the WPF missing-preview blocker to `Use Preview shortlist quarantine before entering confirmation text.` while leaving the core gate wording unchanged.
- Quarantine Execution Gate Help Cue adds a visible hoverable `?` cue beside the WPF gate readout, mirrors concise dynamic tooltip/help text for startup, closed, open, executed, and undone gate states, and expands WPF smoke affordance coverage to twelve cues.
- Full Local MVP Preflight After Gate Help Cue passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with the Quarantine Execution Gate `?` help cue, and whitespace diff check all passed without scanning or modifying real user files.
- Selected Restore Gate Help Cue adds a visible hoverable `?` cue beside the WPF Selected Restore Execution Gate, mirrors concise dynamic tooltip/help text for waiting, closed, open, restored, and custom/real-profile blocked states, and expands WPF smoke affordance coverage to thirteen cues.
- Full Local MVP Preflight After Selected Restore Gate Cue passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with the Selected Restore Execution Gate `?` help cue, and whitespace diff check all passed without scanning or modifying real user files.
- Fixture Checklist Selected Restore Gate Cue updated checklist step 8 and README manual review wording to check the Selected Restore Execution Gate `?` help cue in waiting/closed/open/restored states and watch for visual crowding.
- Restore Manifest Review Surface Decision added ADR 0016: older/discovered Restore Manifests stay in manifest discovery/readiness panes for now, while `Current quarantined` remains current-session-only and not all quarantined history.
- Confirmation Field Help Cues added visible hoverable `?` help cues beside the exact `QUARANTINE` and `RESTORE` confirmation fields and expanded WPF smoke affordance coverage to fifteen cues.
- Full Local MVP Preflight After Confirmation Field Help Cues passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with the new confirmation field `?` help cues, and whitespace diff check all passed without scanning or modifying real user files.
- Help Cue Affordance Feature Brief Alignment updated the original hoverable help-cue feature briefs so they no longer stop their later-packet trail at eleven cues; later manifest-review cue work moved the current affordance snapshot to eighteen cues.
- Restore Manifest Selection Help Cue added a visible hoverable `?` cue beside the selected-manifest picker, mirrors the picker tooltip/help text, and expanded WPF smoke affordance coverage to sixteen cues.
- Quarantine Manifest Discovery Help Cue added a visible hoverable `?` cue beside `Discover manifests`, mirrors the discovery tooltip/help text, and expands WPF smoke affordance coverage to seventeen cues.
- All-Manifest Readiness Help Cue added a visible hoverable `?` cue beside `Preview all-manifest readiness`, mirrors the readiness tooltip/help text, and expands WPF smoke affordance coverage to eighteen cues.
- Manifest Review Control Grouping keeps `Discover manifests`, `Preview all-manifest readiness`, and `Selected manifest` controls visually grouped with their `?` help cues when wrapping.
- Full Local MVP Preflight After Manifest Control Grouping passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with the manifest cue/control wrapping check, and whitespace diff check all passed without scanning or modifying real user files.
- Fresh Thread Handoff Polish refreshed this handoff and `.codex/progress.md` so a new thread starts from the current pushed safety boundary: eighteen hoverable `?` cues, manifest cue/control grouping, full `.cmd` MVP preflight evidence, fixture-only execution/selected restore, and real-profile cleanup still unavailable.
- Manifest Fixture Checklist Split separated the launcher checklist's manifest discovery/all-manifest readiness checks from selected manifest readiness/selected restore gate checks, making the manual fixture prompt easier to follow without changing WPF behavior.
- Redundant Shortlist Overlap Cleanup added `Remove overlapping parents` so redundant parent/child Quarantine Preview blockers can be fixed by removing broader parent rows from Review Shortlist, clearing stale preview/gate state, and re-previewing; WPF smoke coverage passed.
- Compact Scan Header Status moved the Cleanup Scope Safety Note, scan-gate summary, and scan-gate detail into one wrapping WPF header status strip so wide windows recover review height without removing safety wording or changing scan/cleanup behavior; WPF smoke coverage passed.
- Trim AGENTS Workflow Doc reduced `AGENTS.md` from 139 to 74 lines by keeping the repo control-layer rules and linking detailed workflow guidance back to `docs/codex/grill-with-docs.md` and `docs/codex/skillopt-inspired-workflow.md`.
- Real-Profile Quarantine Readiness Contract added ADR 0017, feature/domain docs, and WPF regression coverage that real-profile acknowledgement unlocks only read-only scanning and custom non-fixture clean preview plus exact `QUARANTINE` still cannot execute. First WPF test run was blocked by stale running app process `5492`; after stopping it, the app tests passed. `git diff --check` passed with line-ending normalization warnings only.
- Fixture Checklist Readiness-Contract Boundary updated `Start-MvpFixtureReview.cmd -ChecklistOnly`, README manual review wording, and the MVP readiness-audit follow-up so ADR 0017 blockers stay explicit before any future real-profile work; checklist-only output and whitespace diff passed.
- Real-Profile Quarantine Design Pass added ADR 0018, a feature brief, and domain/glossary terms for execution readiness, root execution safety, pre-execution revalidation, and restore readiness; docs-only whitespace diff passed, and no scan/execution/restore behavior changed. ADR 0018 was later accepted after user decisions.
- Quarantine Execution Readiness Model accepted ADR 0018 with the user's first-phase decisions and added the non-moving core readiness model/tests for fixture-executable, real-profile-candidate, custom-preview-only, caps, root acknowledgement, row/folder eligibility, Undo prerequisite, manual rescan, and Restore Manifest-only records; core tests passed and no WPF execution behavior changed.
- Quarantine Root Execution Safety added the non-moving core root-safety model/tests and optional readiness consumption for fully qualified roots, containment, capacity, action-root collisions, manifest-path collisions, and item destination collisions; core tests passed and no WPF execution behavior changed.
- Pre-Execution Revalidation added the non-moving core revalidation model/tests and optional readiness consumption for source missing/changed, destination collisions, stale action draft mismatches, action/manifest collisions, and root safety recheck; core tests passed and no WPF execution behavior changed.
- Real-Profile Restore Readiness added the non-moving core restore-readiness model/tests and optional readiness consumption for selected-manifest real-profile Undo evidence; core tests passed and no WPF execution behavior changed.
- WPF Execution Readiness Output added read-only readiness contract output to Quarantine Preview and Quarantine Execution Gate, with grouped blockers and WPF smoke assertions; no execution behavior changed.
- Real-Profile Selected Restore Execution Contract added ADR 0019 and a feature brief for the future selected-manifest-only real-profile restore path; no execution behavior changed.
- Real-Profile Selected Restore Regression added WPF smoke coverage that exact `RESTORE` and clean selected readiness for a synthetic real-profile Restore Manifest still leaves selected restore execution unavailable; no execution behavior changed.
- Fixture Checklist Selected Restore Boundary aligned `Start-MvpFixtureReview.cmd -ChecklistOnly` and README wording so the final real-profile/custom blocker prompt names ADR 0017 Quarantine blockers and ADR 0019 selected-restore blockers together.
- Real-Profile Readiness Output Regression added WPF smoke coverage that synthetic exact `C:\Users\moxhe` Quarantine Preview/gate output shows real-profile candidate readiness and missing readiness dimensions while exact `QUARANTINE` still cannot execute; no real-profile scan or movement occurred.
- Real-Profile Child Readiness Output Regression added WPF smoke coverage that synthetic `C:\Users\moxhe\AppData\Local` Quarantine Preview/gate output shows preview-only real-profile child readiness and the exact `C:\Users\moxhe` first-phase scope blocker while exact `QUARANTINE` still cannot execute; no real-profile scan or movement occurred.
- WPF Root Execution Safety Evidence wires read-only Quarantine Root Execution Safety into Quarantine Preview and Quarantine Execution Gate output when a Quarantine Action Draft exists, removing the root-safety-not-checked blocker while keeping revalidation, restore readiness, and real-profile execution blocked.
- WPF Pre-Execution Revalidation Evidence wires read-only Pre-Execution Revalidation into Quarantine Preview and Quarantine Execution Gate output when action-draft and root-safety evidence exist, removing the revalidation-not-checked blocker while keeping restore readiness and real-profile execution blocked.
- WPF Real-Profile Restore Readiness Evidence wires read-only Real-Profile Restore Readiness into selected restore gate output and forward Quarantine readiness consumption when selected-manifest evidence exists, while selected real-profile restore and forward real-profile Quarantine remain blocked.
- Quarantine Readiness Summary adds a compact WPF summary line for waiting, fixture-ready/open, preview-only, stale-executed, and undo-completed readiness states, while detailed readiness evidence stays in the existing preview/gate panes and execution availability stays unchanged.
- Full Local MVP Preflight After Readiness Summary passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with compact Quarantine Readiness Summary wording, and whitespace diff check all passed without scanning or modifying real user files.
- WPF Non-D Quarantine Root Acknowledgement adds a readiness-only checkbox for fully qualified non-`D:` Quarantine Roots, passes it into Quarantine Root Execution Safety evidence, clears stale preview/gate state when changed, and keeps execution availability unchanged.
- Full Local MVP Preflight After Non-D Acknowledgement passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with non-D root readiness acknowledgement wording, and whitespace diff check all passed without scanning or modifying real user files.
- Non-D Root Acknowledgement Help Cue adds a visible hoverable `?` cue beside the WPF acknowledgement, mirrors checkbox tooltip/help text, updates fixture checklist wording, and expands WPF smoke affordance coverage to nineteen cues without changing execution availability.
- Full Local MVP Preflight After Non-D Root Cue passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with the non-D root readiness acknowledgement `?` help cue wording, and whitespace diff check all passed without scanning or modifying real user files.
- User Verification for Non-D Root Acknowledgement Row recorded that the new row feels clear and not crowded.
- Selected Restore Pre-Execution Revalidation added the core-only read-only rediscovery/revalidation model and tests for clean synthetic real-profile selected-manifest evidence, stale missing quarantine paths, and unavailable/non-real-profile blockers; core tests passed and no WPF execution behavior changed.
- WPF Selected Restore Revalidation Evidence shows that revalidation evidence in the WPF selected restore gate for exact real-profile selected Restore Manifests, with WPF smoke coverage for clean evidence and stale missing-quarantine-path blockers; real-profile selected restore remains unavailable.
- Real-Profile Selected Restore Execution later made that exact selected-manifest path executable only after selected readiness, exact `RESTORE`, and immediate revalidation; stale missing-quarantine paths still block and tests do not click real-profile restore.
- Fixture Checklist Selected Restore Revalidation Alignment updates the fixture launcher checklist and README manual review wording so selected restore revalidation evidence is part of the human review path without changing WPF behavior.
- Full Local MVP Preflight After Selected Restore Revalidation Checklist passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with selected restore revalidation wording, and whitespace diff check all passed without scanning or modifying real user files.
- Quarantine Execution Scope Status added plain-language fixture-only versus preview-only scope wording to Quarantine Preview and Quarantine Execution Gate output.
- Fixture Review Checklist Output added a compact terminal checklist to `Start-MvpFixtureReview.ps1`.
- Fixture Review Checklist-Only Mode added `Start-MvpFixtureReview.ps1 -ChecklistOnly`, which prints the same checklist without preflight, fixture creation, or WPF launch.
- Quarantine Approval Boundary Wording added a compact preview/gate line that keeps Review Shortlist and Quarantine Preview separate from cleanup approval.
- Fixture Checklist Approval Boundary updated the launcher checklist to call out Approval boundary plus Execution scope status during fixture review.
- Confirmation Label Wording replaced stale WPF `Required future text` labels with `Required confirmation text` for Quarantine and Selected Restore panes.
- Selected Restore Scope Status added plain-language fixture-only versus preview-only selected restore scope wording plus an approval-boundary line to the Selected Restore Execution Gate.
- Fixture Checklist Selected Restore Scope updated the launcher checklist to call out selected restore Approval boundary plus Execution scope status during fixture review.
- Broad Restore Action Wording updated discovery/readiness/selected-review panes so they say no broad restore action is available and route fixture selected restore through selected readiness and the selected restore gate.
- Fixture Checklist Broad Restore Wording updated the launcher checklist to call out no broad restore action wording during fixture review.
- All-Manifest Restore Wording refined user-facing `broad restore action` wording to `all-manifest restore action` in WPF and checklist surfaces.
- Visible Row Shortlist Labels renamed bulk shortlist buttons/status text to `Shortlist visible rows` / `Remove visible rows` without changing the read-only display-window behavior.
- Scope-Specific Scan Gate Ready Wording made ready scan-gate summaries/tooltips distinguish fixture gated cleanup actions from unavailable real-profile/custom cleanup execution.
- Execution Control Tooltip Clarity updated disabled Quarantine and selected-restore controls so their tooltips match fixture-only gates and real-profile/custom blockers.
- Undo Quarantine Domain Consistency corrected current domain wording so Undo Quarantine Executor is described as used by current-fixture undo and fixture-only selected restore, while real-profile/all-manifest restore stays unavailable.
- Restore Manifest Wording Polish aligned current-facing manifest wording with Restore Manifest language in the current-fixture undo tooltip, README manual check, WPF smoke assertion messages, and domain docs without changing restore availability.
- Selected Manifest Readiness Label Polish renamed the visible selected-readiness action to `Preview selected manifest readiness` and aligned current checklist/docs/tests wording without changing restore availability.
- All-Manifest Readiness Label Polish renamed the all-manifest readiness action to `Preview all-manifest readiness` and aligned current checklist/docs/tests wording without changing restore availability.
- Readiness Scope Tooltip Clarity added scope/approval-boundary tooltips to `Preview all-manifest readiness` and `Preview selected manifest readiness`, including disabled-state tooltip support for selected manifest readiness.
- Visible-Row Shortlist Tooltip Clarity added disabled-state scope/no-file-modified/not-cleanup-approval tooltips to `Shortlist visible rows` and `Remove visible rows`.
- Review Toolbar Report and Preview Tooltip Clarity added disabled-state report-only, in-memory-only, and dry-run tooltips to `Export shortlist`, `Clear shortlist`, `Preview quarantine`, and `Export preview`.
- Selection Browse Tooltip Clarity added disabled-state path-only/preview-only/no-approval tooltips to Cleanup Scope and Quarantine Root `Browse...` controls.
- Selected-Row Action Tooltip Clarity added disabled-state review-only/inspection-only tooltips to selected-row shortlist, copy, focus, preview, and Explorer actions.
- Review Navigation and Export Tooltip Clarity added disabled-state report-only/no-rescan/in-memory tooltips to `Export CSV`, `Clear search`, `Reset view`, `Previous rows`, and `Next rows`.
- Review Toolbar Automation Help Text added matching WPF automation help text for `Export CSV`, `Clear search`, `Reset view`, `Previous rows`, and `Next rows`.
- Review Report and Preview Automation Help Text added matching WPF automation help text for `Export shortlist`, `Clear shortlist`, `Preview quarantine`, and `Export preview`.
- Selected Row Action Automation Help Text added matching WPF automation help text for `Add to shortlist`, `Remove`, `Copy path`, `Show children`, `Show descendants`, `Preview file`, and `Open in Explorer`.
- Selection Browse Automation Help Text added matching WPF automation help text for Cleanup Scope and Quarantine Root `Browse...`.
- Execution and Readiness Automation Help Text added matching WPF automation help text for Quarantine execution, current-fixture undo, restore readiness, selected restore gate, and fixture selected restore controls, plus a tooltip for `Preview selected restore gate`.
- Visible Row Shortlist Automation Help Text added matching WPF automation help text for `Shortlist visible rows` and `Remove visible rows`.
- Scan Gate Automation Help Text added disabled-state tooltip support and matching dynamic WPF automation help text for `Scan`.
- Quarantine Root Input Automation Help Text added matching WPF automation help text for the typed Quarantine Root field.
- Cleanup Scope Input Automation Help Text added tooltip and matching WPF automation help text for the typed Cleanup Scope field.
- Storage Review Search Input Automation Help Text added matching WPF automation help text for the search field with prefix examples and read-only/no-rescan/no-cleanup-approval wording.
- Review Lens Filter Help Text added tooltip and matching WPF automation help text for Storage Review Filter buttons plus Type, Size, and Category filters.
- Safety Summary Shortcut Help Text added disabled-state tooltip and matching WPF automation help text for Safety Summary review shortcut buttons.
- Real Profile Acknowledgement Help Text added tooltip and matching WPF automation help text for the real-profile preflight and fixture-review acknowledgement checkbox.
- Real Profile Acknowledgement Help Cue added a visible hoverable `?` cue beside the real-profile acknowledgement, mirrors the checkbox tooltip/help text, hides for fixture/custom scopes, and expanded WPF smoke affordance coverage to eight cues.
- Manifest Discovery and Selection Help Text added disabled-state tooltip and matching WPF automation help text for `Discover manifests` and Restore Manifest selection.
- Scan Cancel Help Text added disabled-state tooltip and matching WPF automation help text for `Cancel`.
- Manual Fixture Show Children and Clipboard Fix made `Show children` status name `Reset view` as the way back and catches busy-Clipboard Copy path failures instead of crashing.
- User completed manual fixture review through fixture Quarantine execution and observed that, after a rescan, the moved file disappears from Storage Scan rows as expected.
- Preserve Current Fixture Undo After Rescan keeps `Undo fixture quarantine` available after that post-execution rescan until undo is attempted.
- User feedback from the same pass: Quarantine Preview success was easy to miss because it appeared only in the status bar; consider stronger visible feedback.
- User suggested a separate quarantined-files area so moved files remain reviewable/restorable independently of the current Storage Scan grid.
- User expected one button for the Review Shortlist rather than per-file approval. Shortlist-Level Quarantine Wording renamed the fixture action to `Quarantine included shortlist`, renamed preview to `Preview shortlist quarantine`, and extended smoke coverage to two included rows moving/restoring together.
- Current-Session Quarantined Review moved the Quarantine shortlist gate out of the right detail scroll panel, grouped it with Quarantine Root, and added `Quarantined` / `Back to scan rows` main-grid switching for current in-memory Restore Manifest entries still in `Moved` state.
- User review showed the new Quarantine shortlist area made the grid too small; Collapsible Review Panels made Safety Summary and Quarantine shortlist collapsible and constrained verbose gate details.
- User verified the collapsible panels worked and asked for closed-panel summaries; Collapsed Panel Summaries added compact Safety Summary counts and Quarantine shortlist/preview/current-quarantine/undo state to the Expander headers.
- Quarantine Preview Inline Status added a compact preview/readiness line inside the Quarantine shortlist panel so preview success is visible near the controls, not only in the status bar.
- Review Grid Mode Status added a compact line above the main grid that identifies Storage Scan rows versus current-session quarantined rows, points to `Back to scan rows`, and warns when scan rows may be stale after fixture Quarantine execution.
- Fixture Checklist Review Polish Alignment updated `Start-MvpFixtureReview.ps1 -ChecklistOnly` prompts to include collapsible panel review, inline preview readiness, Review Grid Mode Status, and `Quarantined` / `Back to scan rows`.
- Quarantined View Control Help Text added dynamic disabled/enabled tooltip and automation help text for `Quarantined` and `Back to scan rows`; Current Quarantined Label later renamed the visible grid switch to `Current quarantined`.
- Quarantine Preview Status Styling added lightweight semantic styling to the inline Quarantine Preview status: neutral waiting, success ready/completed, warning blockers/stale, and error preview failure.
- Fixture Checklist Preview Status Styling Alignment updated `Start-MvpFixtureReview.ps1 -ChecklistOnly` prompts to include styled inline Quarantine Preview readiness states.
- Quarantine Preview Error Style Coverage added WPF smoke coverage for invalid Quarantine Root preview attempts using the inline error status style.
- Full Local MVP Preflight After Review Polish passed `Invoke-MvpPreflight.ps1`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed without scanning or modifying real user files.
- Review Grid Mode Status Styling added neutral/informational/warning styling to distinguish ordinary scan rows, stale scan rows, and current-session quarantined review.
- Fixture Checklist Grid Mode Styling Alignment updated `Start-MvpFixtureReview.ps1 -ChecklistOnly` prompts to include styled Review Grid Mode Status states.
- Retire Stale Grid-Mode Styling Follow-ups updated feature briefs so they no longer list Review Grid Mode Status styling as future work after that packet landed.
- Collapsed Panel Header Help Text mirrors Safety Summary and Quarantine shortlist closed-header summaries into tooltip and automation help text with read-only/not-cleanup-approval wording.
- Full Local MVP Preflight After Header Help passed `Invoke-MvpPreflight.ps1`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed without scanning or modifying real user files.
- Current Quarantined Label renamed the visible current-session quarantined grid switch to `Current quarantined`, while older/discovered Restore Manifest review remains in manifest discovery/readiness panes.
- Current Quarantined Count Label updates that grid switch to show `Current quarantined (N)` when current-session moved entries are available, while keeping empty/disabled states compact and older/discovered Restore Manifest review in manifest discovery/readiness panes.
- Full Local MVP Preflight After Current Quarantined Count passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed without scanning or modifying real user files.
- Full Local MVP Preflight After Current Label passed `Invoke-MvpPreflight.ps1`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed without scanning or modifying real user files.
- User ran the latest manual fixture review checklist through steps 1-11 and reported that all checked behavior worked; user also agreed that useful collapsed panel header summaries are desirable.
- Review Grid Mode Status Help Text mirrored dynamic grid-mode wording into tooltip and automation help text with read-only, no-rescan, no-file-modified, no-restore, and not-cleanup-approval boundaries.
- Full Local MVP Preflight After Grid Mode Help passed `Invoke-MvpPreflight.ps1`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed without scanning or modifying real user files.
- Quarantine Preview Status Help Text mirrored dynamic inline preview wording into tooltip and automation help text with read-only, no-create, no-move, no-restore, no-delete, and not-cleanup-approval boundaries.
- Quarantine Shortlist Header Styling added lightweight closed-header styling for neutral, needs-preview/blocked/stale, preview-ready/undo-completed, and current-quarantined states without changing cleanup execution or restore availability.
- Safety Summary Header Styling added lightweight closed-header styling for waiting/neutral and safety-signal warning states without changing scan behavior, cleanup execution, or scan gates.
- Full Local MVP Preflight After Safety Header Styling passed `Invoke-MvpPreflight.ps1`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed without scanning or modifying real user files.
- Collapsed Header State Help Text added textual `Header state:` wording to Safety Summary and Quarantine shortlist header tooltip/help text so collapsed-header state is not color-only.
- Full Local MVP Preflight After Header State Help passed `Invoke-MvpPreflight.ps1`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed without scanning or modifying real user files.
- Status State Help Text added textual `Status state:` wording to Review Grid Mode Status and inline Quarantine Preview tooltip/help text so styled status state is not color-only.
- Collapsed Header Summary Labels made collapsed Safety Summary and Quarantine Shortlist headers start with their visible panel names while preserving compact summary counts, state styling, tooltip/help text, and cleanup boundaries.
- Full Local MVP Preflight After Header Summary Labels passed `Invoke-MvpPreflight.ps1`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed without scanning or modifying real user files.
- Review Shortlist Safety Mix Help Text mirrored the dynamic safety mix into tooltip/help text with no-rescan, no-file-modified, no-Quarantine-readiness-proof, no-storage-savings-proof, and not-cleanup-approval boundaries.
- Review Mix Help Text mirrored Review Mix and Matched Review Mix into tooltip/help text with no-rescan, no-file-modified, no-storage-savings-proof, and not-cleanup-approval boundaries.
- Review Mix Help Cues added visible circular `?` help cues beside Review Mix and Matched Review Mix so their mirrored tooltip/help text is easier to discover.
- Review Shortlist Safety Mix Help Cue added a visible circular `?` help cue beside Review Shortlist Safety Mix so its mirrored tooltip/help text is easier to discover before Quarantine Preview.
- Full Local MVP Preflight After Safety Cue passed `Invoke-MvpPreflight.ps1`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed without scanning or modifying real user files.
- Review Grid Mode Status Help Cue added a visible circular `?` help cue beside Review Grid Mode Status so its mirrored status-state tooltip/help text is easier to discover above the main grid.
- Quarantine Preview Status Help Cue added a visible circular `?` help cue beside the inline Quarantine Preview readiness/status line so its mirrored dry-run/status-state tooltip/help text is easier to discover before any fixture execution gate.
- Full Local MVP Preflight After Help Cues passed `Invoke-MvpPreflight.ps1`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed without scanning or modifying real user files.
- Collapsed Header Help Cues added visible circular `?` help cues beside Safety Summary and Quarantine Shortlist collapsed headers so their mirrored header summary/state tooltip/help text is easier to discover.
- Full Local MVP Preflight After Header Cues passed `Invoke-MvpPreflight.ps1`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed without scanning or modifying real user files.
- Hoverable Help Cue Affordance added the Windows help cursor and short tooltip delay to the seven existing circular `?` cues without changing scan, Quarantine, restore, or manifest behavior.
- Execution-Policy Friendly Fixture Launcher added `.\tools\Start-MvpFixtureReview.cmd`, which invokes the existing PowerShell launcher with process-scoped `-ExecutionPolicy Bypass` and forwards arguments such as `-ChecklistOnly` and `-SkipPreflight`.
- Full Local MVP Preflight After CMD Launcher passed `Invoke-MvpPreflight.ps1`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed, and preflight now suggests `.\tools\Start-MvpFixtureReview.cmd -SkipPreflight` as the next manual fixture step.
- Execution-Policy Friendly Tool Wrappers added `.\tools\Invoke-MvpPreflight.cmd` and `.\tools\New-StorageScanSmokeFixture.cmd`, making process-scoped bypass wrappers available for all human-facing PowerShell tools.
- Full Local MVP Preflight Through CMD Wrapper passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed without scanning or modifying real user files.
- Fixture Checklist Hoverable Help Cues aligned `Start-MvpFixtureReview.ps1 -ChecklistOnly` and README fixture launcher wording with the current hoverable `?` help-cue affordance.
- Full Local MVP Preflight After Hoverable Checklist passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed without scanning or modifying real user files.
- Hoverable Help Cue Affordance Coverage added WPF smoke assertions that all tracked circular `?` cues use the Help cursor and prompt tooltip delay.
- Full Local MVP Preflight After Help Cue Coverage passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed without scanning or modifying real user files.
- Full Local MVP Preflight After Scan Gate Cue passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, and whitespace diff check all passed without scanning or modifying real user files.
- Read-Only Readiness Builder Guard added core source-level regression coverage that read-only readiness/revalidation builders do not call movement executors, write Restore Manifests, or perform direct filesystem movement/write operations; core tests and whitespace diff passed.
- WPF Execution Bridge Source Guard added core source-level regression coverage that WPF movement executor calls stay isolated to `ExecuteQuarantineForCurrentPreview`, `ExecuteSelectedRestoreForCurrentSelection`, and `UndoQuarantineForCurrentExecution`, and remain behind the current gate/fixture checks; core tests and whitespace diff passed.
- Full Local MVP Preflight After Source Guards passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with selected restore revalidation and ADR 0017/0019 blocker wording, and whitespace diff check all passed without scanning or modifying real user files.
- Quarantine Readiness Summary Preview-Only Wording changed the compact WPF summary for real-profile/custom preview-only scopes from `Current build can execute: no` to `movement unavailable`, while keeping detailed preview/gate readiness evidence unchanged; WPF app tests and whitespace diff passed.
- Real-Profile Child Readiness Summary Wording Coverage extended the synthetic `C:\Users\moxhe\AppData\Local` WPF smoke test to assert compact preview-only summary wording, warning styling, and mirrored tooltip/help text for real-profile-child scopes; WPF app tests and whitespace diff passed.
- Fixture Checklist Movement-Unavailable Summary Alignment updated `Start-MvpFixtureReview.cmd -ChecklistOnly` and README manual review wording so manual fixture review prompts now ask reviewers to check preview-only `movement unavailable` wording in the compact Quarantine Readiness Summary.
- Full Local MVP Preflight After Movement Summary Checklist passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with preview-only movement-unavailable wording, and whitespace diff check all passed without scanning or modifying real user files.
- Real-Profile Quarantine Approval Evidence added a read-only core model proving exact `QUARANTINE` is necessary but not sufficient, defaulting current-build movement availability to unavailable, carrying readiness blockers forward, rejecting custom/child scopes, and staying inside the read-only readiness-builder source guard; core tests and whitespace diff passed.
- WPF Real-Profile Approval Evidence Output surfaced that model in the Quarantine Execution Gate for custom, exact synthetic real-profile, and real-profile-child preview-only scopes while keeping fixture gate output uncluttered; WPF app tests and whitespace diff passed.
- Fixture Checklist Approval Evidence Alignment updated `Start-MvpFixtureReview.cmd -ChecklistOnly` and README manual review wording so reviewers check for read-only Real-Profile Quarantine Approval Evidence with `Can approve real-profile movement: no` alongside ADR 0017/0019 blockers.
- Full Local MVP Preflight After Approval Evidence Checklist passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with `Can approve real-profile movement: no`, and whitespace diff all passed without scanning or modifying real user files.
- WPF Approval Evidence Display-Only Guard added source-level coverage that WPF builds approval evidence only for gate display, leaves movement availability unavailable, skips fixture scopes, and keeps button enablement tied to `_currentQuarantineExecutionGate.CanExecute`; core tests and whitespace diff passed.
- Restore Manifest Review Summary added compact WPF summary coverage for discovery, selected readiness, selected restore gate closed/open states, all-manifest readiness counts, and selected fixture restore result guidance; WPF app tests, checklist-only output, and whitespace diff passed.
- Full Local MVP Preflight After Manifest Summary passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with `Restore Manifest review summary states/tooltips`, and whitespace diff all passed without scanning or modifying real user files.
- WPF Real-Profile First-Phase Blocker Output added representative readiness-blocker selection before WPF pane truncation and synthetic exact real-profile WPF coverage for row-cap, byte-cap, no-category, and strict-descendant blockers while keeping real-profile movement unavailable.
- Full Local MVP Preflight After First-Phase Blockers passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with Real-Profile Quarantine Approval Evidence wording, and whitespace diff all passed without scanning or modifying real user files.
- Fixture Checklist First-Phase Blocker Alignment updated checklist step 10 and README manual-review wording so ADR 0018 first-phase limits stay visible in the next manual review prompt; checklist-only output and whitespace diff passed.
- Quarantine Readiness Summary Key Blockers added concise `Key blockers:` labels to preview-only compact summaries and synthetic exact real-profile WPF coverage for `10-row cap`, `1 GB cap`, `no-category rows`, and `strict descendant checks` while keeping real-profile movement unavailable.
- Full Local MVP Preflight After Key Blockers passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with ADR 0018 first-phase limits, and whitespace diff all passed without scanning or modifying real user files.
- Fixture Checklist Readiness Key Blockers updated checklist step 6 and README manual-review wording so the next visible fixture pass checks compact Quarantine Readiness Summary `Key blockers` labels; checklist-only output and whitespace diff passed.
- Quarantine Readiness Key-Blocker Label Rules consolidated the compact summary key-blocker mapping into one shared WPF rule table while preserving visible wording and real-profile/custom movement blockers; WPF app tests passed.
- Full Local MVP Preflight After Label Rules passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with compact summary key-blocker wording, and whitespace diff all passed without scanning or modifying real user files.
- Preview-Only Key-Blocker Summary Coverage added WPF smoke assertions for custom `custom scope preview-only`, exact real-profile `pre-execution revalidation` / `restore readiness` / `current build unavailable`, and real-profile-child `exact profile scope` compact-summary labels while keeping behavior unchanged.
- Fixture Checklist Preview-Only Key Labels updated the printed fixture checklist and README manual-review wording to name `custom scope preview-only`, `exact profile scope`, `pre-execution revalidation`, `restore readiness`, `current build unavailable`, `10-row cap`, `1 GB cap`, `no-category rows`, and `strict descendant checks`; checklist-only output and whitespace diff passed.
- Fresh Thread Handoff Polish After Readiness Labels refreshed this handoff and `.codex/progress.md` so the next thread starts from the current pushed safety boundary: 19 hoverable `?` cues, latest checklist-only evidence after the key-label checklist packet, latest full `.cmd` MVP preflight evidence after the label-rules packet, fixture-only execution/selected restore, and real-profile cleanup/restore still unavailable.
- Fixture Checklist Fixture-First Boundary clarified `Start-MvpFixtureReview.cmd -ChecklistOnly` and README wording so the final real-profile/custom blocker check does not ask reviewers to scan `C:\Users\moxhe` during the fixture pass; checklist-only output and whitespace diff passed.
- WPF Panel Cleanup added quieter scan, summary, review-control, Quarantine, and selected-path panels; Safety Summary and Quarantine Shortlist now start collapsed while preserving header state/help cues; WPF app tests, checklist-only output, and whitespace diff passed.
- WPF Tabbed Workbench moved Scan metrics, Safety Summary, Review controls, Quarantine controls/readiness, and Main Grid into horizontal tab pages; Main Grid is selected by default, Safety Summary and Quarantine pages start expanded, and WPF app tests, checklist-only output, and whitespace diff passed.
- WPF Header Scan Metrics moved the sparse Scan-tab totals into a compact wrapping header strip, removed the Scan tab, kept Safety Summary / Review / Quarantine / Main Grid tabs with Main Grid selected by default, and WPF app tests, checklist-only output, and whitespace diff passed.
- WPF Header Shortlist Metrics added compact header Review Shortlist size/file/folder totals with read-only/not-savings/not-approval tooltip/help text; WPF app tests, checklist-only output, and whitespace diff passed.
- WPF Header Shortlist Metric Placement reordered the header metric area so Review Shortlist totals sit before `Total size` when space allows; WPF app tests, checklist-only output, and whitespace diff passed.
- Full Local MVP Preflight After Header Polish passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with latest header-placement wording, and whitespace diff all passed without scanning or modifying real user files.
- Main Grid Auto-Focus for Grid-Switching Actions made Safety Summary shortcuts, `Current quarantined`, and `Back to scan rows` select Main Grid after changing visible rows; WPF app tests passed.
- Main Grid Active Review Lens Summary added compact active-filter/search/focus text above Storage Scan rows, hides it for current-session quarantined rows, and mirrors the existing Filter Summary with read-only/no-rescan/no-file-modified tooltip/help text; WPF app tests, checklist-only output, and whitespace diff passed.
- Full Local MVP Preflight After Active Lens passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with active-lens wording, and whitespace diff all passed without scanning or modifying real user files.
- Header Layout Visual Approval Notes recorded the user's 2026-06-01 approval of the compact wide-header layout, tightened Main Grid active-lens wording to `above Storage Scan rows`, and kept the remaining manual fixture work focused on Quarantine/readiness and selected-restore review rather than re-litigating the approved header direction.
- Fixture Checklist Grid-Mode Cue Wording clarified checklist step 7 so Review Grid Mode Status owns the hoverable `?` cue and state-naming tooltip/help text, while Main Grid Active Review Lens Summary is checked as Storage Scan-row orientation text that hides for current-session quarantined rows; checklist-only output and whitespace diff passed.
- Full Local MVP Preflight After Fixture Checklist Cue Wording passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, fixture checklist-only output with clarified Review Grid Mode Status / Main Grid active-lens wording, and whitespace diff all passed without scanning or modifying real user files.
- Live Product Readiness Roadmap added `docs/features/2026-06-01-live-product-readiness-roadmap.md` to make the remaining gates explicit: visible fixture acceptance, fresh real-profile read-only retest, selected real-profile restore, first real-profile Quarantine execution, recovery confidence, packaging, and later deletion/history decisions; checklist-only output passed without launching WPF or touching real user files.
- Fixture Acceptance Notes Template added `-WriteAcceptanceNotes` to `Start-MvpFixtureReview.cmd`; checklist-only notes output writes ignored `.local\fixture-review-acceptance\fixture-acceptance-*.md` templates with pass/issue/not-checked slots, while `-WhatIf` does not write notes.
- Fixture Checklist Section Grouping added section headers to checklist-only output and generated acceptance notes: Scan header and gate; Safety Summary, Review, and Main Grid; Quarantine Preview and fixture Quarantine; Restore Manifest review and selected restore; Real-profile and custom blockers.
- Full Local MVP Preflight After Section Grouping passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, sectioned fixture checklist-only output, and whitespace diff all passed without scanning or modifying real user files.
- Preflight Fixture Notes Next Step updated successful `Invoke-MvpPreflight.cmd` output to suggest `.\tools\Start-MvpFixtureReview.cmd -SkipPreflight -WriteAcceptanceNotes`, aligning the next visible fixture pass with ignored acceptance notes; focused preflight, checklist-notes, and whitespace checks passed without scanning or modifying real user files.
- Fixture Notes Launcher Wording Alignment updated current-facing README, roadmap, and fixture-notes feature briefs so the post-preflight visible fixture pass consistently uses `.\tools\Start-MvpFixtureReview.cmd -SkipPreflight -WriteAcceptanceNotes`; checklist-notes and whitespace checks passed without scanning or modifying real user files.
- Fixture Acceptance Evidence Header added repository, Git branch/commit, required preflight command, visible fixture command, preflight/worktree checkboxes, and local-not-cleanup-history wording to ignored `.local` fixture acceptance notes; checklist-notes, notes inspection, `-WhatIf`, and whitespace checks passed without scanning or modifying real user files.
- Full Local MVP Preflight After Evidence Header passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, sectioned fixture checklist-only output, and whitespace diff all passed without scanning or modifying real user files, and the success output printed the notes-enabled next manual fixture command.
- Live Product Roadmap Evidence Alignment updated the roadmap's manual fixture acceptance row and completion notes so the evidence header and full-preflight-after-header gate are named together; checklist-notes/header inspection and whitespace checks passed without scanning, launching WPF, moving, restoring, deleting, or modifying real-profile files.
- Fixture Acceptance Build Context Header added .NET SDK, WPF app project, WPF app target framework, and WPF enabled fields to ignored `.local` fixture acceptance notes; checklist-notes/header inspection, `-WhatIf`, and whitespace checks passed without scanning, launching WPF, moving, restoring, deleting, or modifying real-profile files.
- Full Local MVP Preflight After Build Context Header passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, sectioned fixture checklist-only output, and whitespace diff all passed without scanning or modifying real user files, and the success output printed the notes-enabled next manual fixture command.
- Fixture Acceptance Current Commit Notes Preview passed `cmd.exe /c tools\Start-MvpFixtureReview.cmd -ChecklistOnly -WriteAcceptanceNotes`, printed the grouped checklist, and wrote ignored `.local\fixture-review-acceptance\fixture-acceptance-20260601-112248.md` stamped with `main`, commit `fd8e1d4`, .NET SDK `8.0.421`, WPF app project/target framework/WPF flag, required preflight, visible fixture command, local evidence checkboxes, and not-cleanup-history wording without preflight, fixture creation, WPF launch, scan, movement, restore, deletion, or cleanup history.
- Fixture Acceptance Notes Summary Helper added `.\tools\Summarize-FixtureAcceptanceNotes.cmd`; latest and explicit-path runs summarized `.local\fixture-review-acceptance\fixture-acceptance-20260601-112248.md` with overall result, checklist totals, and not-recorded items without launching WPF, scanning, moving, restoring, deleting, or creating cleanup history.
- Fixture Acceptance Summary Prompt Preview updated `.\tools\Summarize-FixtureAcceptanceNotes.cmd` output so open checklist items show compact recorded notes or prompt previews; latest and explicit-path runs passed without launching WPF, scanning, moving, restoring, deleting, or creating cleanup history.
- Fixture Acceptance Evidence Checkbox Summary updated `.\tools\Summarize-FixtureAcceptanceNotes.cmd` output so acceptance-evidence checkboxes for preflight passed and worktree clean/intentional state are visible; latest and explicit-path runs showed both as not recorded for the current checklist-only preview without launching WPF, scanning, moving, restoring, deleting, or creating cleanup history.
- Fixture Acceptance Completion Check added `.\tools\Summarize-FixtureAcceptanceNotes.cmd -RequireComplete`; the current checklist-only preview correctly exits non-zero with blockers for unrecorded evidence, missing overall result, and 10 not-recorded checklist items, without launching WPF, scanning, moving, restoring, deleting, or creating cleanup history.
- Fixture Acceptance Post-Pass Guidance updated `.\tools\Start-MvpFixtureReview.cmd -ChecklistOnly -WriteAcceptanceNotes` output so newly written notes show exact follow-up summary and completion-check commands, and preflight success output points to those printed commands.
- Fixture Acceptance Notes Embedded Commands updated generated acceptance notes so the notes file itself includes exact follow-up summary and completion-check commands, while preserving the read-only/no-WPF/no-scan/no-movement/no-cleanup-history boundary.
- Full Local MVP Preflight After Embedded Notes passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, sectioned checklist-only output, and whitespace diff all passed without launching WPF, scanning real-profile files, moving, restoring, deleting, or creating cleanup history.
- Fixture Acceptance Notes Worktree Stamp added `Worktree status at notes creation` to generated ignored notes and summary output; checklist-notes output and explicit summary passed without launching WPF, scanning, moving, restoring, deleting, or creating cleanup history.
- Fixture Acceptance Clean Worktree Notes Preview generated `.local\fixture-review-acceptance\fixture-acceptance-20260601-122940.md` from clean `main` at `f181627`; the header stamped `Worktree status at notes creation: clean`, explicit summary printed it, and explicit `-RequireComplete` returned exit code 1 as expected because the visible-pass notes are not filled yet.
- Full Local MVP Preflight After Clean Notes Preview passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd` after `8529a91`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, sectioned checklist-only output, and whitespace diff all passed without launching WPF, scanning real-profile files, moving, restoring, deleting, or creating cleanup history.
- Full Local MVP Preflight After Current Evidence Wording Alignment passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd` after `466ad79`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, sectioned checklist-only output, and whitespace diff all passed without launching WPF, scanning real-profile files, moving, restoring, deleting, or creating cleanup history.
- Full Local MVP Preflight After Checklist-Only Next Step passed `cmd.exe /c tools\Invoke-MvpPreflight.cmd` after `71cf15a`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, sectioned checklist-only output with the exact visible fixture next-step block, and whitespace diff all passed without launching WPF, scanning real-profile files, moving, restoring, deleting, or creating cleanup history.

## Best next work

Prefer a small packet. The user-reported visible fixture pass, fresh real-profile read-only retest, sacrificial selected real-profile restore trust test, first tiny exact real-profile Quarantine live trust batch, selected restore recovery loop, portable v1 packaging, and latest read-only daily evidence refresh all look good. Broad/all-manifest real-profile Undo, custom/non-exact real-profile Quarantine, permanent deletion, and cleanup history remain unavailable. Good candidates:

1. Use the README Daily Local Use section or `tools\Invoke-DailyLocalReadiness.cmd` for the read-only daily accepted-package and Restore Manifest evidence check; add `-IncludeFixtureAcceptanceNotes` when formal fixture notes status should be visible and `-RequireFixtureAcceptanceComplete` only when incomplete fixture notes should fail the check; use `tools\Invoke-RealProfileNextBatchReview.cmd` before considering another tiny exact real-profile Quarantine batch when both the exact-profile evidence preset and manual WPF checklist should be shown together; use `tools\Invoke-RealProfileQuarantineReadiness.cmd -RequireNextBatchEvidence` when only evidence is needed; use `tools\Show-RealProfileNextBatchChecklist.cmd` when only the post-preset checklist is needed and remember it now repeats fixture-notes incomplete handling; use `tools\Invoke-RealProfileQuarantineReadiness.cmd -AllCleanupScopes` only when fixture manifests should also be displayed outside the preset; use `tools\Start-AcceptedLocalRelease.cmd` when only a package launch command is needed; use `tools\Summarize-RestoreManifests.cmd -CleanupScope "C:\Users\moxhe" -RequireAnyDisplayed -RequireNoDisplayedUndoWork` to check exact-profile displayed undo-work evidence; use `tools\Summarize-RestoreManifests.cmd -CleanupScope "C:\Users\moxhe" -RecoveryReviewOnly -ShowEntries` to inspect exact real-profile recovery-review debt; decide whether shortcut/installer automation is worth a later explicit user-approved packaging packet.
2. Keep any future real-profile Quarantine batch exact `C:\Users\moxhe`, exact `QUARANTINE`, capped at 10 rows / 1 GB, Likely safe + Quarantine candidate only, strict descendant checks, immediate pre-execution revalidation, Quarantine Root safety, Restore Manifest-only durable record, manual rescan guidance, no permanent deletion, no all-manifest restore, and no cleanup history.
3. If stricter local fixture evidence is useful, run `.\tools\Summarize-FixtureAcceptanceNotes.cmd -Path ".local\fixture-review-acceptance\fixture-acceptance-20260601-132126.md"` or the strict `-RequireComplete` variant first; incomplete output now prints the exact recorder command. Run that `Record-FixtureAcceptanceNotes.cmd -Path ... -RecordManualAcceptance` command only after an actual all-pass visible fixture review; fill notes manually instead when there were issues or not-checked items.
4. If the user wants broader restore/history browsing, start a new Grill with Docs packet for a dedicated restore/history surface instead of expanding `Current quarantined` into all quarantined history.

## Commands

```powershell
git -c safe.directory='D:/Codex/Windows File Cleaner' status --short --branch
.\tools\Invoke-MvpPreflight.ps1
.\tools\Invoke-MvpPreflight.cmd
dotnet build WindowsFileCleaner.sln
dotnet run --project tests\WindowsFileCleaner.Tests\WindowsFileCleaner.Tests.csproj
dotnet run --project tests\WindowsFileCleaner.App.Tests\WindowsFileCleaner.App.Tests.csproj
dotnet run --project src\WindowsFileCleaner.App
.\tools\New-StorageScanSmokeFixture.cmd
.\tools\New-RealProfileSelectedRestoreTrustManifest.cmd
.\tools\Start-MvpFixtureReview.cmd
.\tools\Start-MvpFixtureReview.cmd -ChecklistOnly
.\tools\Start-MvpFixtureReview.cmd -ChecklistOnly -WriteAcceptanceNotes
.\tools\Start-MvpFixtureReview.cmd -SkipPreflight -WriteAcceptanceNotes
.\tools\Summarize-FixtureAcceptanceNotes.cmd
.\tools\Summarize-FixtureAcceptanceNotes.cmd -RequireComplete
.\tools\Record-FixtureAcceptanceNotes.cmd -RecordManualAcceptance
.\tools\Publish-LocalRelease.cmd
.\tools\Test-LocalRelease.cmd
.\tools\Test-LocalRelease.cmd -RequireCurrentCommit
.\tools\Start-LocalRelease.cmd -ChecklistOnly -RequireCurrentCommit
.\tools\Start-LocalRelease.cmd -Fixture -ChecklistOnly -RequireCurrentCommit
.\tools\Start-LocalRelease.cmd -ChecklistOnly -RequireCurrentCommit -WriteAcceptanceNotes
.\tools\Start-AcceptedLocalRelease.cmd -PrintOnly
.\tools\Start-AcceptedLocalRelease.cmd -Fixture -PrintOnly
.\tools\Invoke-DailyLocalReadiness.cmd
.\tools\Invoke-DailyLocalReadiness.cmd -IncludeFixtureAcceptanceNotes
.\tools\Invoke-DailyLocalReadiness.cmd -RequireFixtureAcceptanceComplete
.\tools\Invoke-RealProfileQuarantineReadiness.cmd
.\tools\Invoke-RealProfileQuarantineReadiness.cmd -RequireNextBatchEvidence
.\tools\Invoke-RealProfileQuarantineReadiness.cmd -RequireAnyDisplayedRestoreManifest -RequireNoDisplayedUndoWork
.\tools\Show-RealProfileNextBatchChecklist.cmd
.\tools\Invoke-RealProfileNextBatchReview.cmd
.\tools\Summarize-LocalReleaseAcceptanceNotes.cmd
.\tools\Record-LocalReleaseAcceptanceNotes.cmd -RecordManualAcceptance
.\tools\Summarize-RestoreManifests.cmd -CleanupScope "C:\Users\moxhe" -RequireAnyDisplayed -RequireNoDisplayedUndoWork
.\tools\Summarize-RestoreManifests.cmd -CleanupScope "C:\Users\moxhe" -RecoveryReviewOnly -ShowEntries
.\tools\Summarize-RestoreManifests.cmd -CleanupScope "C:\Users\moxhe" -UndoWorkOnly
.\tools\New-StorageScanSmokeFixture.ps1
.\tools\Start-MvpFixtureReview.ps1
.\tools\Start-MvpFixtureReview.ps1 -ChecklistOnly
```

## Startup prompt

```text
We are continuing Windows File Cleaner in D:\Codex\Windows File Cleaner.

Read AGENTS.md, .codex/progress.md, README.md, docs/codex/thread-handoff.md, docs/domain/context.md, docs/domain/glossary.md, and relevant docs/features/ and docs/decisions/ before implementing.

Current state: main is pushed through the latest Shortcut Installer Deferral Decision packet. The app is a C#/.NET 8 WPF local Windows cleanup reviewer for `C:\Users\moxhe`. Storage Scan is read-only. Fixture Quarantine execution, current-fixture undo, fixture selected restore, exact real-profile selected restore, and first-phase exact real-profile Quarantine execution exist; the sacrificial selected real-profile restore trust test succeeded by user report, the first tiny exact real-profile Quarantine live trust batch succeeded by user report, and selected restore recovery for that first-live manifest later succeeded by user report with `Restored 1, failed 0`; follow-up rediscovery showed restored/already restored, rescan completed normally, and the restored `pip\cache\http\b\c` path appeared again. Restore Manifest discovery/review now lives in the Quarantine tab's `Restore Manifest Review` panel, not the Main Grid detail pane. Portable v1 packaging exists through `.\tools\Publish-LocalRelease.cmd`, producing ignored self-contained `.local\releases` folder/zip artifacts with executable SHA-256 metadata, zip `.sha256` sidecars, release-local `README-FIRST.txt`, and normal/fixture launch `.cmd` scripts; `.\tools\Start-AcceptedLocalRelease.cmd -PrintOnly` prints the accepted package command without launching WPF and repeats that the accepted release launch command is the daily path instead of any debug-build desktop shortcut. Any desktop shortcut that still points at the debug build is dev-only context, not accepted-package evidence or the recommended daily launch path. ADR 0020 keeps accepted package launch commands as the v1 daily path and defers installed shortcut or installer automation to a later explicit user-approved packaging packet. `.\tools\Invoke-DailyLocalReadiness.cmd` verifies accepted package notes, can optionally summarize or strictly require Fixture Acceptance Notes, runs one accepted package verification pass, prints accepted normal/fixture launch commands, and prints Restore Manifest Summary output without creating shortcuts, installing anything, launching WPF, scanning, moving, restoring, deleting, approving cleanup, or creating cleanup history. Latest daily readiness on current `main` at `5b2a982` passed with `.\tools\Invoke-DailyLocalReadiness.cmd -IncludeFixtureAcceptanceNotes -CleanupScope "C:\Users\moxhe" -RequireAnyDisplayedRestoreManifest -RequireNoDisplayedUndoWork`: accepted package evidence passed with the expected accepted-package/current-HEAD warning, optional Fixture Acceptance Notes status remained visible and formally incomplete, exact-profile display showed 4 manifests, displayed undo work was 0, displayed recovery review was 2, and accepted normal/fixture launch commands printed in print-only mode. Latest full next-batch evidence at `d257090` passed with `.\tools\Invoke-RealProfileQuarantineReadiness.cmd -RequireNextBatchEvidence`: full MVP preflight passed, accepted package verification passed with the expected accepted-package/current-HEAD warning, optional Fixture Acceptance Notes status remained visible and formally incomplete with recorder guidance printed, exact-profile display showed 4 manifests, displayed undo work was 0, displayed recovery review was 2, focused recovery-review showed the two older NVIDIA `DXCache` attempts, and focused undo-work showed zero exact-profile matches. `.\tools\Invoke-RealProfileQuarantineReadiness.cmd -RequireNextBatchEvidence` remains the recommended evidence-only preset before another tiny exact real-profile batch; it requires exact `C:\Users\moxhe`, includes Fixture Acceptance Notes status, requires displayed Restore Manifest evidence, and requires zero displayed undo-work manifests. `.\tools\Show-RealProfileNextBatchChecklist.cmd` prints `.\tools\Invoke-RealProfileNextBatchReview.cmd` as the recommended combined review before the separate evidence preset and manual WPF checklist, and it now includes the Fixture Acceptance Notes handling reminder. `.\tools\Invoke-RealProfileNextBatchReview.cmd` runs that preset and then prints the checklist in one terminal-only flow; `-SkipMvpPreflight` is for smoke checks only. Current accepted local package baseline is `.local\releases\windows-file-cleaner-v20260602-011556` at commit `bc9b869`, with completed ignored acceptance notes `.local\release-acceptance\release-acceptance-20260602-011743.md`; `.\tools\Summarize-LocalReleaseAcceptanceNotes.cmd -RequireComplete` passes for that local evidence. Latest optional fixture-notes evidence confirmed `.local\fixture-review-acceptance\fixture-acceptance-20260601-132126.md` remains formally unfilled; strict `-RequireFixtureAcceptanceComplete` fails as expected while preflight/worktree evidence, overall result, and all 10 checklist statuses are not recorded. `.\tools\Summarize-RestoreManifests.cmd` can summarize action-scoped Restore Manifests read-only and focus displayed manifests by exact Cleanup Scope, recovery-review state, or undo-work state. Broad/all-manifest real-profile Undo Quarantine, custom/non-exact real-profile Quarantine, custom selected restore, permanent deletion, and persisted cleanup history remain intentionally unavailable.

Fresh startup-prompt delta: the latest packet is now Shortcut Installer Deferral Decision. ADR 0020 keeps accepted package launch commands as the v1 daily path and defers installed shortcut or installer automation until a later explicit user-approved packaging packet defines target selection, stale-target handling, created artifacts, removal behavior, and read-only verification. This was docs/ADR only and did not create shortcuts, install anything, publish a package, launch WPF, scan, move, restore, delete, approve cleanup, write Restore Manifests, or create cleanup history.

Recent completed packets added ADR 0017/0018/0019 readiness contracts; synthetic exact real-profile, real-profile first-phase blocker, and real-profile-child WPF readiness-output coverage without scanning `C:\Users\moxhe`; exact real-profile selected restore implementation that folds Selected Restore Pre-Execution Revalidation blockers into the gate and reruns revalidation immediately before `UndoQuarantineExecutor.Undo`; first-phase exact real-profile Quarantine execution wiring that requires readiness, approval evidence, and immediate revalidation before `QuarantineExecutor.Execute`; stale exact real-profile selected restore and missing-source real-profile Quarantine coverage without clicking real movement in tests; a source guard keeping movement executor calls isolated to known gated bridge methods; the approved horizontal tabbed workbench/header layout; fixture acceptance notes tooling plus a terminal recorder and incomplete-summary recorder guidance for completed all-pass fixture notes; user-reported fixture and real-profile read-only retest acceptance; portable v1 packaging; portable release verification; portable release launch scripts; portable release checklist-only acceptance output; portable release acceptance notes tooling; verifier/current-commit evidence prefill for release acceptance notes; exact normal/fixture launch commands in release acceptance notes and summaries; a terminal recorder for completed local release acceptance notes; and an accepted-package launcher for daily use after docs-only commits. WPF smoke coverage tracks 19 circular help cues.

Current handoff baseline supersedes older historical evidence references in that packet list: the user-reported visible fixture pass looked good, the user-reported fresh real-profile read-only retest worked well, the latest clean notes file is `.local\fixture-review-acceptance\fixture-acceptance-20260601-132126.md` from `433064e` with the checklist still unfilled, and the latest full `.cmd` MVP preflight passed after `71cf15a`.

User verification: real-profile scan works; search typing was sluggish, then debounced search fixed it; the status-bar message is enough; the non-`D:` acknowledgement row feels clear and not crowded; the tabbed/header UI now looks much better, including the compact wide-header Review Shortlist totals before scan totals; the manual fixture visual pass looks good; the fresh real-profile read-only retest worked well end to end; the user approved the ADR 0019 selected restore implementation path and then the ADR 0017/0018 first real-profile Quarantine packet; the first tiny exact real-profile Quarantine live trust batch succeeded with `moved 1, failed 0`; the first selected restore attempt failed safely because directory restore needed a cross-volume fallback; the retry restored successfully and the user confirmed rediscovery/rescan/restored-path evidence afterward. Keep this as a local-first, safety-gated app. Do not move, delete, quarantine, or restore real-profile files unless the user explicitly asks for the specific action.

Fresh ignored fixture acceptance notes evidence: `.local\fixture-review-acceptance\fixture-acceptance-20260601-132126.md` was generated from clean `main` at `433064e` with `Worktree status at notes creation: clean`; explicit summary passed and reported the formal checklist is still unfilled.

Latest full preflight evidence: `cmd.exe /c tools\Invoke-MvpPreflight.cmd` passed after `71cf15a`; restore, build, core tests, WPF app tests, fixture `-WhatIf`, sectioned checklist-only output with the exact visible fixture next-step block, and whitespace diff passed without launching WPF, scanning real-profile files, moving, restoring, deleting, or creating cleanup history. Plain checklist-only output now repeats the exact next visible fixture command and the checklist-only no-preflight/no-fixture/no-WPF/no-scan/no-movement boundary.

Codex desktop note: action-link approval UI may throw `Unable to find Electron app...`; rely on terminal command output and normal git commands instead of action links when that happens.

Please inspect current git status first, then choose the best next small packet. Recovery confidence for the first-live manifest is now user-reported successful, including rediscovery, rescan, and restored-path confirmation; Restore Manifest discovery/review is now findable in the Quarantine tab; `.\tools\Invoke-DailyLocalReadiness.cmd` runs the read-only accepted-package plus Restore Manifest terminal check and can optionally include fixture notes status with `-IncludeFixtureAcceptanceNotes` or fail incomplete notes with `-RequireFixtureAcceptanceComplete`; `.\tools\Invoke-RealProfileNextBatchReview.cmd` runs the exact-profile-only next-batch evidence preset and then prints the manual WPF checklist before any future tiny exact real-profile batch review; `.\tools\Invoke-RealProfileQuarantineReadiness.cmd -RequireNextBatchEvidence` remains available when only evidence is needed; `.\tools\Show-RealProfileNextBatchChecklist.cmd` remains available when only the post-preset WPF checklist is needed; `.\tools\Summarize-RestoreManifests.cmd` can summarize action-scoped Restore Manifests read-only from the terminal, focus displayed manifests by exact Cleanup Scope with `-CleanupScope`, focus recovery-review manifests with `-RecoveryReviewOnly`, focus undo-work manifests with `-UndoWorkOnly`, fail full-root evidence checks with `-RequireNoRecoveryReview` or `-RequireNoUndoWork`, and fail displayed-only evidence checks with `-RequireAnyDisplayed`, `-RequireNoDisplayedRecoveryReview`, or `-RequireNoDisplayedUndoWork`; portable v1 packaging is available through `.\tools\Publish-LocalRelease.cmd`, accepted package `.local\releases\windows-file-cleaner-v20260602-011556` has complete ignored notes at `.local\release-acceptance\release-acceptance-20260602-011743.md`, and `.\tools\Start-AcceptedLocalRelease.cmd -PrintOnly` prints the accepted package command without launching WPF. Next either use the daily readiness check for local review, run the next-batch review wrapper before a future user-clicked tiny exact batch, summarize exact real-profile recovery evidence with `-CleanupScope "C:\Users\moxhe"`, decide whether shortcut/installer automation deserves a later explicit user-approved packet, complete formal fixture notes if useful, or guide only another tiny exact `C:\Users\moxhe` batch after readiness review plus WPF ADR 0018 readiness, exact `QUARANTINE`, approval evidence, and immediate revalidation visible. Otherwise prefer Quarantine Preview/readiness clarity, scan-gate discoverability, Review Shortlist safety context, or handoff/docs cleanup. Permanent deletion, cleanup history, broad/all-manifest restore, custom real-profile Quarantine, and non-exact real-profile movement remain intentionally unavailable. Run the narrowest relevant checks, update docs, and commit/push at clean packet boundaries.
```
